<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xcheng.xclogger"
    android:sharedUserId="android.uid.system">

    <!-- 现有权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 新增权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/xcloggericon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XCLogger">

        <!-- 主界面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 配置界面 -->
        <activity android:name=".ui.XcLoggerConfigActivity" />

        <!-- 开发者界面 -->
        <activity android:name=".processctr.DeveloperActivity" />

        <!-- 日志捕获服务 -->
        <service
            android:name=".service.LogCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync">
        </service>
        <service
            android:name=".service.RemoteBindService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xcheng.xclogger.REMOTE_BIND" />
            </intent-filter>
        </service>
        <service
            android:name=".filemanager.FileCompressService"
            android:enabled="true"
            android:exported="false" />

        <!-- 广播接收器 -->
        <receiver
            android:name=".receiver.XcLoggerBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xcheng.xclogger.ADB_CMD" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xcheng.xclogger.FILE_COMPRESS" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xcheng.xclogger.CTRL_REQUEST" />
            </intent-filter>
        </receiver>
    </application>
</manifest>