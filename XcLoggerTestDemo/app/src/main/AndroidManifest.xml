<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.xcheng.xcloggertestdemo.permission.CONTROL_STRESS"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.xcheng.xcloggertestdemo.permission.CONTROL_STRESS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="com.xcheng.xclogger" />
        <package android:name="com.ko.xclogger" />
        <package android:name="com.xcheng.xcloggertestdemo.fork1" />
        <package android:name="com.xcheng.xcloggertestdemo.fork2" />
        <package android:name="com.xcheng.xcloggertestdemo.fork3" />
        <package android:name="com.xcheng.xcloggertestdemo.fork4" />
        <package android:name="com.xcheng.xcloggertestdemo.fork5" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XcLoggerTestDemo"
        tools:targetApi="31">

        <!-- Main entry -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Test pages -->
        <activity android:name=".ConfigTestActivity" android:exported="false" />
        <activity android:name=".CompressTestActivity" android:exported="false" />
        <activity android:name=".AutoTestActivity" android:exported="false" />
        <activity android:name=".StressTestActivity" android:exported="false" />

        <!-- Bridge service: binds XCLogger AIDL for all pages -->
        <service
            android:name=".XcLoggerTestService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Persistent foreground service for adjustable log output stress testing -->
        <service
            android:name=".StressTestService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Broadcast receiver for automation testing -->
        <receiver
            android:name=".AutoTestReceiver"
            android:exported="true" />
    </application>

</manifest>
